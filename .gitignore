node_modules
dist
# npm run e2e and e2e:cloud: their builds, and what Playwright writes
/e2e/.dist/
/e2e/.dist-cloud/
/playwright-report/
/playwright-report-cloud/
/test-results/
/test-results-cloud/
.DS_Store
*.local
server/drafter.db
dev-dist

# docs stay local-only — never uploaded (the root README is the one exception)
*.md
!/README.md
.claude/

# Node the Xcode Cloud scripts download (ios/App/ci_scripts); never committed.
/.drafter-ci/
.cursor/
