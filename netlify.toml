[build]
  command = "npm run build"
  publish = "dist"

[functions]
  node_bundler = "esbuild"

# AI proxy first, then the SPA fallback
[[redirects]]
  from = "/api/ai"
  to = "/.netlify/functions/ai"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
