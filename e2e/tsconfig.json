{
  // The browser tests and their config, type-checked by `npm run e2e` before
  // Playwright runs them (it strips the types without reading them).
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "types": ["node"]
  },
  "include": [".", "../playwright.config.ts", "../playwright.cloud.config.ts"]
}
